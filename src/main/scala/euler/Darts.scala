package euler

object Problem109 {
  
  val singles = (1 to 20).toList ++ List(25)
  val doubles = (2.to(40).by(2)).toList ++ List(50)
  val triples = (3.to(60).by(3)).toList
  
  def apply(): Int = {
    0
  }
  
  def generateCheckouts(): List[Int] = {
    List()
  }
  
  
}